export default function LikedSongs() {

  return (
    <div>
      <h1>Liked Songs</h1>
      <ul>
        {/* {likedSongs.map((song) => (
          <li key={song.id}>{song.title}</li>
        ))} */}
      </ul>
    </div>
  );
}
